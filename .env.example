# ── BoardGame AI Moderator ────────────────────────────────────────────
# Copy this file to `.env` and fill in your API key.
#
# MISTRAL_API_KEY is the only required variable. Everything else has
# sensible defaults.

# Mistral La Plateforme API key (required for AI features)
MISTRAL_API_KEY=

# Override model identifiers if needed
# MISTRAL_LARGE_MODEL=mistral-large-latest
# MISTRAL_SMALL_MODEL=mistral-small-latest
# MAGISTRAL_MEDIUM_MODEL=magistral-medium-latest
# MISTRAL_OCR_MODEL=mistral-ocr-latest
# MISTRAL_EMBED_MODEL=mistral-embed
# MISTRAL_MODERATION_MODEL=mistral-moderation-latest

# Server
# HOST=0.0.0.0
# PORT=8000
# DEBUG=false

# Frontend CORS origins (comma-separated)
# CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]
