# GEMINI_API_KEY: Optional — only needed if using Gemini AI directly.
# The backend now handles all AI calls via Mistral API.
GEMINI_API_KEY=""

# VITE_API_URL: URL of the FastAPI backend (default: http://localhost:8000)
# When running locally, the Vite dev server proxies /api requests automatically.
VITE_API_URL="http://localhost:8000"
