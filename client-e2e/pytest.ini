[pytest]
; Isolate this suite from root addopts and prepend client-e2e for package imports.
addopts = -v --durations=5 --disable-warnings
; Use SIGALRM so hung clients fail without introducing a fork-unsafe thread.
timeout = 60
timeout_method = signal
